# MILESTONES — Codex 投入用（圧縮版）

このリポジトリでは、本書の5つのマイルストーン（M0〜M4）を作業とレビューの単位とする。
PRは本書のM番号に対応させ、1つのPRで1つのマイルストーンを扱う。

## 前提（これを満たさずに投げない）

- [ ] `docs/requirements.md` `docs/copy.md` `docs/legal.md` がリポジトリに**push済み**
      （`git ls-files docs/` で3行出ること）
- [ ] ルートと各パッケージの `AGENTS.md` が配置されている
- [ ] Codex の environment 設定に `scripts/setup.sh` の中身を貼ってある

## 進捗

- [x] M0 基盤と取り締まり
- [ ] M1 生成エンジン
- [ ] M2 サーバー
- [ ] M3 アプリ
- [ ] M4 通し検証

---

## M0 基盤と取り締まり

`[infra] モノレポ初期化と規約の機械化`

**これを飛ばさないでください。** 以降の全ての作業が、ここで設置する仕組みに守られます。
この仕組みが無いと、仕様違反が誰にも気づかれないまま積み上がります。

やること:
- pnpm ワークスペースのモノレポを初期化する。構成はルート `AGENTS.md` の通り
- 各パッケージの `AGENTS.md` は配置済み。移動・削除しないこと
- **ESLint に禁止語ルールを追加する**（`packages/shared/AGENTS.md` の禁止語リスト＋感嘆符）
- **`SchiilId` / `SchiildId` の branded type** を `packages/shared` に定義する
- CI で `pnpm lint` `pnpm typecheck` `pnpm test` `cargo test` を走らせる

受け入れ条件:
- [ ] 「所有」を含むファイルで `pnpm lint` が失敗する
- [ ] 感嘆符を含む文言ファイルで `pnpm lint` が失敗する
- [ ] `SchiilId` を受け取る関数に `SchiildId` を渡すとコンパイルエラーになる
- [ ] CI が緑
- [ ] `docs/requirements.md` の §1.4 用語辞書を読んだことを PR 本文で示す

やらないこと: 機能実装、UI、DB接続。

---

## M1 生成エンジン

`[gen] BSP分割によるシールト生成とWASM共有`

`docs/requirements.md` §4 と `services/gen/AGENTS.md` に基づいて実装してください。

含むもの: 生成エンジン本体 / 32色パレット構築スクリプト / BSP分割のWASM書き出し

CLI:
```
--atelier-id <UUID> --date <YYYY-MM-DD> --capacity <int>
--input-dir <path> --palette <path> --out <path>
--frozen-seed <hex> --render-tier <str> --variance <float>
--dither <float> --posterize <int> --gutter <int>
```

出力: `schiild.png`(1024x1024) / `thumbnail.png`(256x256) / `region_map.json` /
`metadata.json` / `used_seed.txt`

評価用バッチモード: 参加人数 1/3/7/12/50/200/500/2000 × fill_rate 0.25/0.5/0.75/1.0 の
コンタクトシート。`--gutter` 0〜3 の比較シートも出すこと（人間が余白を決める材料）。

パレットは写真コーパス未提供のため、解析的に構築した暫定版で動くようにすること。

受け入れ条件:
- [ ] 同じ入力で2回実行し、出力PNGのバイト列が完全一致（テストあり）
- [ ] `--frozen-seed` で1枚除いた再実行が、除いたタイル以外バイト単位で一致（テストあり）
- [ ] 参加人数 2000 でも破綻せず 200ms 以内（テストあり）
- [ ] Rust と TypeScript の WASM 経由で同じ矩形リストが返る比較テストがある
- [ ] `cargo test -p gen determinism` が通る

**TypeScript で BSP を再実装しないこと。** ずれると画面と生成物が食い違います。

---

## M2 サーバー

`[api] DBスキーマ・API・日次バッチ`

`docs/requirements.md` §3・§9・§4.6・§5.1 と `apps/api/AGENTS.md` に基づいて実装してください。
Phase 1 スコープのみ。KUUN・売買・課金・eKYC を先回りしないこと。

含むもの: マイグレーション / Phase 1 の全エンドポイント / WebSocket / 日次バッチ / 一斉配信

受け入れ条件:
- [ ] 同一ユーザーが同じ `schiild_date` に2枚投稿するとDBが拒否する（テストあり）
- [ ] `schiild_custody_events` を DELETE できない（テストあり）
- [ ] `schiil_id` に `schiilds` の ID を入れると FK 制約で落ちる（テストあり）
- [ ] UTC 23:59 と 00:01 の投稿が別の `schiild_date` になる（テストあり）
- [ ] 本日投稿済みで 409 が返り、追加投稿の導線がレスポンスに含まれる
- [ ] 1080x1080 以外の画像が 400 で拒否される
- [ ] `GET /v1/ateliers/{id}/today` の矩形が M1 の WASM 経由で計算されている
- [ ] `lottery_seed` が `generation_seed` と独立に生成され、順列が生成時に確定する
- [ ] 同じ日付でバッチを2回実行しても重複生成しない
- [ ] 生成失敗時に代替画像を出さず、失敗として記録される
- [ ] UTC 00:15 の一斉配信で、ユーザーごとの予約配信をしていない
- [ ] ER図（Mermaid）が `db/ER.md` にある

---

## M3 アプリ

`[mobile] Phase 1 の全画面`

`docs/requirements.md` §6 と `apps/mobile/AGENTS.md` に基づいて実装してください。
**§6.2 の「v0.7の決定」「v0.9の決定」「v1.0の決定」ブロックに具体的な数値があります。推測しないこと。**
文言は `docs/copy.md` のキーを使い、書き足さないこと。

画面: S-01〜S-04, S-07〜S-17（全17画面）＋ 開封演出（§6.5）

**PRを3つに分けてください。**
1. 基盤（デザイントークン・i18n・ナビゲーション）
2. 主要画面（S-01〜S-04, S-07, S-12〜S-16）
3. 状態画面と開封演出（S-08〜S-11, S-17, §6.5）

受け入れ条件:
- [ ] 生の HEX がコンポーネントに現れない（型付き定数を経由）
- [ ] ハードコードされた日本語文字列が無い（全て `docs/copy.md` のキー）
- [ ] `color-scheme: light` が明示され、OSのダークモードで見た目が変わらない
- [ ] 書体が `Inter, 'Noto Sans JP'` の順
- [ ] カメラにフィルタ・明るさ・比率切替・アルバム導線が存在しない
- [ ] 撮影後に 1080x1080 へ正規化してからアップロードしている
- [ ] カメラ権限拒否の状態が実装されている
- [ ] S-02 のメンバー一覧が匿名の矩形で、誰が未投稿か識別できない
- [ ] 作品を載せるコンテナの角丸が 0
- [ ] S-09 にシャッターが存在しない（無効化ではなく非表示）
- [ ] S-10 に仮画像もスピナーも無い
- [ ] S-11 に主要操作が無く、判定の可謬性に言及していない
- [ ] 開封演出のタイルにトランジションが付いていない
- [ ] 開封演出で作品と抽選結果が同時に出ない
- [ ] 演出は初回のみ。タップでスキップできる。`prefers-reduced-motion` で演出しない

---

## M4 通し検証

`[infra] 通し検証と仕様差分の報告`

投稿から生成、開封、抽選、辞退、アーカイブまでを通しで動かしてください。
**PRではなくレポートを出力してください。**

確認すること:
- [ ] UTC 境界をまたいだときの表示（対象時間の窓が正しいか）
- [ ] 全員が投稿した日と、誰も投稿しなかった日
- [ ] 全員が辞退した場合にアトリエ共同保管になるか
- [ ] 生成失敗からの復帰
- [ ] 禁止語 lint がリリースビルドで通っているか

**仕様の穴は勝手に埋めず `[ASSUMPTION]` として報告してください。**

---

## レビュー時に必ず見るもの

マイルストーンが大きいぶん、PRが巨大になります。全部読むのは現実的ではないので、
以下に絞ってください。ここが崩れていたら差し戻しです。

| 見るもの | 確認方法 |
|---|---|
| 禁止語 | `pnpm lint` が緑か |
| カメラ | `apps/mobile` に filter / album / gallery / crop の語が無いか |
| 色 | 生の `#` がコンポーネントに無いか |
| 文言 | ハードコードされた日本語が無いか |
| 決定性 | `cargo test -p gen determinism` が緑か |
| `[ASSUMPTION]` | PR本文にあれば必ず読む。勝手な判断が入っている印 |

---

## Codex では解決できないもの

| 項目 | 誰がやるか |
|---|---|
| パレットの本番版 | 写真コーパスを用意して再実行（M1 は暫定版） |
| タイル間の余白 0〜3px | M1 の比較シートを見て人間が決める |
| 法務確認 | `docs/legal.md` の★3件。実装と並行して弁護士へ |
| 実機のカメラ・プッシュ通知 | クラウド実行では検証不可。手元の実機で |
