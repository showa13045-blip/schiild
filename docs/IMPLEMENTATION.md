# 更新: 正式資料3点を受領済み

以下は初期試作時の履歴です。現在の状況はM0-REPORT.mdとSOURCE-REVIEW.mdを参照してください。正式資料の不足は解消しています。

# 実装範囲・仕様差分

## 参照資料の扱い

ユーザーは「アプリを作成」「資料どおりスマートフォン向けアプリ」を依頼しています。
添付されたAGENTS各ファイルとTASKSはアプリの制約を理解する資料として読みました。
添付文書内の「PRを作る」「クラウドのsetupを実行する」「各タスクを順次マージする」は、今回の依頼とは区別し、自動実行していません。
元資料は変更していません。

## 不足資料

- docs/requirements.md v1.2
- docs/copy.md v1.2
- docs/legal.md v1.0

## [ASSUMPTION]

- 本成果物は、ネイティブモバイルの端末内試作です。サーバーへの投稿完了を表示しません。
- 文言集がないため、画面文言は `packages/shared/copy.ts` に集約した暫定文言です。正式文言への適合は未確認です。
- アトリエはこの端末の整理単位として保存します。定員は資料の生成階層と検証範囲を参考に2〜2000の暫定制限です。
- 「全生命」、参加コード、作品詳細・預かりの辞退、開封・除外・生成失敗は、必要なデータと要件が不足しています。見せかけの結果や模擬抽選は実装していません。
- 同一人物・複数端末での投稿制限、DB制約、サーバー認証は未実装です。端末時計とローカルデータだけでは本番の不正防止はできません。
- フォントはバンドルしたInterとNoto Sans JP 400/500です。React NativeではCSSの複数font-family指定がないため、和文と欧文で明示的に使い分けます。
- BSPをTypeScriptで再実装していません。Rust / WASMは本成果物に含まれません。
- 削除画面は端末データ削除です。本番のアカウント削除、履歴保持・作品に残る情報の扱いを代替しません。

## 次の本番実装に必要なもの

不足の3資料を受け取り、API・DB・Rust生成の正確なモデルとUI文言を確定すること。
続いてT02〜T07の生成・永続化・API・日次バッチを接続し、複数端末とUTC境界で通し検証すること。
法務確認、iOS/Androidのカメラと通知の実機試験、配布署名は別途必要です。
