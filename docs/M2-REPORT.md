# M2 着手状況

M2は`m2-server`ブランチで着手。M1のPR #2が未マージのため、そのブランチを土台に差分を分離する。M2全体は未完了。

## 今回の基盤

PostgreSQL 16のPhase 1マイグレーション、チェックサム付き適用ツール、ローカルseed、ER図、実DB制約テストを追加した。

確認済み: 同日2枚目の拒否、UTC 23:59/00:01の別日判定、セッションtimezoneからの独立、作品IDを写真FKへ入れた際の拒否、他人の写真の拒否、重複投稿・アトリエ作品・全生命作品の拒否、履歴DELETE/TRUNCATE/更新の拒否、アプリ用ロールからの削除拒否。マイグレーションは2回実行して二重適用されないことを確認した。

## 続く実装

- Firebase認証とPhase 1 HTTPエンドポイント。今回、未認証の仮APIは公開していない。
- JPEG寸法・形式検証、EXIF除去、S3保存、写真と投稿の同時トランザクション。
- M1 WASMによるtoday、匿名のWebSocket更新。
- Redis/BullMQの日次ジョブ、生成・抽選の保存、辞退による繰下げ。
- UTC 00:15の一斉通知と生成失敗通知。

上記のAPI・ワーカーをDBに接続し、MILESTONES.mdのM2受け入れ条件を満たすまで、M2完了とは扱わない。外部サービスの接続情報は環境変数で設定し、試験では外部APIをモックする。


## 2026-09-09 API・ワーカーの追加

Firebase IDトークン検証、Phase 1 HTTPエンドポイント、JPEG検証・EXIF除去、投稿トランザクション、todayの実WASM、匿名WebSocket、実Rust生成、独立した抽選と辞退、UTC日次キュー・一斉通知outboxを追加した。todayの暫定構図と専用シードからの検証可能な抽選方式はユーザー承認済み。
HTTP/DB統合試験で、認証拒否、同時投稿201/409、画像400、保存失敗時のロールバック、EXIF除去、追加投稿、UTC境界、匿名WebSocket、実生成と二重実行、公開抽選検証、全員辞退、全生命作品、失敗記録、一斉通知の時刻と冪等性を確認。

未完了: 審査プロバイダーの承認・接続、実Firebase/S3/Redis/FCM設定、CloudFrontの画像配信。運用用起動は未構成の画像審査を503で拒否する。モック承認を本番既定にしない。M2全体の完了・デプロイ可能とは扱わない。接続とテストの範囲はapps/api/README.mdに記録。
